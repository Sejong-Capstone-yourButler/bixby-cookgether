structure (GetOrderListResults) {
  description (주문 내역을 불러온다.)
  property (ok) {
    type (Ok)
    min (Required) max (One)
  }
  property (orders){
    type(Orders)
    min(Optional) max(Many)
  }
}
